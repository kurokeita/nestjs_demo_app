# This configuration file was automatically generated by Gitpod.
# Please adjust to your needs (see https://www.gitpod.io/docs/introduction/learn-gitpod/gitpod-yaml)
# and commit this file to your remote git repository to share the goodness with others.

# Learn more from ready-to-use templates: https://www.gitpod.io/docs/introduction/getting-started/quickstart

tasks:
  - init: |
      openssl genrsa -out storage/oauth/private.pem 2048
      openssl rsa -in storage/oauth/private.pem -pubout -out storage/oauth/public.crt
      yarn install
      yarn prisma:generate
      yarn build
      gp env DATABASE_URL="mysql://root@localhost:3306/nestjs_app"
      gp env APP_PORT=3000
      gp env JWT_EXPIRATION_TIME=900
      gp env REFRESH_TOKEN_EXPIRATION_TIME=864000
      eval $(gp env -e)
      yarn prisma:dev
      yarn prisma db seed
    command: |
      yarn prisma:dev
      yarn start:debug

image: gitpod/workspace-mysql

github:
  prebuilds:
    master: true
    branches: false
    pullRequests: true
    addCheck: true
    addComment: true
